# =============================================================================
# Base Environment Configuration (.env)
# =============================================================================
# This file has the LOWEST priority in Vite's load order.
#
# LOAD ORDER (from lowest to highest priority):
#   1. .env                    ← YOU ARE HERE (loaded in ALL modes)
#   2. .env.local              (personal overrides for ALL modes)
#   3. .env.[mode]             (e.g., .env.development, .env.production)
#   4. .env.[mode].local       (highest priority - e.g., .env.development.local)
#
# Example: If VITE_API_BASE_URL exists in both .env and .env.local,
#          the value from .env.local WINS (higher priority).
#
# All variables MUST be prefixed with VITE_ to be exposed to the client.
# Reference: https://vite.dev/guide/env-and-mode
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
VITE_APP_NAME=TodoApp

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Default to localhost for development
VITE_API_BASE_URL=http://localhost:3000/api
VITE_TIMEOUT=10000

# -----------------------------------------------------------------------------
# Performance & Network
# -----------------------------------------------------------------------------
VITE_API_RETRY_COUNT=3

# -----------------------------------------------------------------------------
# UI Configuration
# -----------------------------------------------------------------------------
VITE_SNACKBAR_MAX_COUNT=3
VITE_SNACKBAR_AUTO_HIDE=4000

# -----------------------------------------------------------------------------
# Development & Debugging
# -----------------------------------------------------------------------------
VITE_ENABLE_DEBUG=false
VITE_ENABLE_MSW=false
