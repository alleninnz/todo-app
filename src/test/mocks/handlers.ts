import type { HttpHandler } from 'msw'

/**
 * TODO: Add mock API handlers that mirror the production endpoints.
 * Group them by feature (tasks, lists, auth) to keep the mocks easy to navigate.
 */
export const handlers: HttpHandler[] = []

/**
 * TODO: When implementing handlers, remember to cover success and error states
 * so UI tests can validate optimistic updates, retry flows, and error messaging.
 */
